//Initialize variables
timer0 = 300;		//duration between spawns (frames)
timer0_min = 280;	//minimum duration between spawns 
timer0_dec = 1;		//amount to reduce from duration each spawn
alarm[0] = timer0;	//set alarm to trigger spawn

timer1 = 400;		//duration between spawns (frames)
timer1_min = 360;	//minimum duration between spawns 
timer1_dec = 1;		//amount to reduce from duration each spawn
alarm[1] = timer1;	//set alarm to trigger spawn
